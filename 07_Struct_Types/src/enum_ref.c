#include <stdio.h>

/**
 * An enumeration is a set of named integer constants that specify the legal 
 * values that a variable of that type can have.
 * */


typedef enum Boolean {
	TRUE = 1, 
	FALSE = 0
} bool;

int main(int argc, char** argv) {


	return 0;
}